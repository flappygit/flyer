# Flyer Root Start
FlyerRoot:
<FlyerRoot>:
	FlyerHeader
# Flyer Root End

<FlyerHeader>:
	canvas.before:
		Rectangle:
			size: self.size
			pos: self.pos
			source: 'flyer_logo.png'
		Color: 
			rgba: 0.964, 0.776, 0.047, 1
		Rectangle:
			size: self.size[0], self.size[1]*0.05
